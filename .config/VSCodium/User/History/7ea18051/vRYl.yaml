apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: minio-secret.patch.yaml
    target:
      kind: Secret
      name: govforms2-workflow-core
