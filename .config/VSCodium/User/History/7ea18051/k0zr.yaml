apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

secretGenerator:
  - name: empfangsclient-plus-secrets
    envs:
      - ./mixins/empfangsclient-plus-secrets.env
    options:
      disableNameSuffixHash: true
