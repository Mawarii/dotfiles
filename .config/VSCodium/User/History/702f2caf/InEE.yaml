apiVersion: apps/v1
kind: Deployment
metadata:
  name: govforms2-client-deployment
  labels:
    k8s-env: develop
spec:
  template:
    spec:
      imagePullSecrets:
        - name: gitlab-registry-govforms2-client
        - name: gitlab-registry-version-sidecar
