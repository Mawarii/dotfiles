apiVersion: apps/v1
kind: Deployment
metadata:
  name: govforms2-client
  labels:
    k8s-env: develop
    spec:
      imagePullSecrets:
        - name: gitlab-registry-client
        - name: gitlab-registry-version-sidecar
