apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: domibus-config-pvc
spec:
  storageClassName: hcloud-volumes
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
