apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: empfangsclient-ecc-version-ingress
  labels:
    k8s-env: "pre-stage"
spec:
  ingressClassName: "nginx"
  rules:
    - host: "version.staging.efa.owl-it.de"
      http:
        paths:
          - pathType: Prefix
            path: "/empfangsclient-ecc-zdi"
            backend:
              service:
                name: empfangsclient-ecc
                port:
                  name: version
