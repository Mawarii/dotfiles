apiVersion: v1
kind: ConfigMap
metadata:
  name: matomo-postinit
data:
  custom-post-init.sh: |
    #!/bin/bash
    grep -q 'force_ssl = 1' /bitnami/matomo/config/config.ini.php || sed -i '/\[General\]/a force_ssl = 1' /bitnami/matomo/config/config.ini.php;
