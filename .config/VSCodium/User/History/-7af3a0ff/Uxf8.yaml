apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-gateway
  labels:
    k8s-env: pre-stage
spec:
  template:
    spec:
      imagePullSecrets:
        - name: gitlab-registry-identity-gateway
