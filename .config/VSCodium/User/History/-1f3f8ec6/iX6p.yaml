---
# Source: matomo/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matomo
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: "nginx"
  rules:
    - host: matomo-new.pre-stage.wspdev.de
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: matomo
                port:
                  name: http
  tls:
    - hosts:
        - "matomo-new.pre-stage.wspdev.de"
      secretName: matomo-new.pre-stage.wspdev.de-tls
