apiVersion: mariadb.mmontes.io/v1alpha1
kind: MariaDB
metadata:
  name: mariadb
spec:
  rootPasswordSecretKeyRef:
    key: ROOT_PASSWORD
    name: hsi-intranet-drupal-mariadb
  passwordSecretKeyRef:
    key: PASSWORD
    name: hsi-intranet-drupal-mariadb
  envFrom:
    - configMapRef:
        name: config-hsi-intranet-drupal-mariadb
