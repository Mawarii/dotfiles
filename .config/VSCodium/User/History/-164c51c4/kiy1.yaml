---
- name: Create empty auth.log file
  ansible.builtin.file:
    path: /var/log/auth.log
    state: touch
    mode: '0640'

- name: Start and enable systemd services
  ansible.builtin.systemd_service:
    name: fail2ban
    enabled: true
    state: started
