kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

resources:
  - deplyoment.yaml
  - service.yaml

secretGenerator:
  - name: pdfreactor-license
    files:
      - licensekey.txt

generatorOptions:
  disableNameSuffixHash: true
