stages:
  - build

build:
  stage: build
  image:
    name: harbor.dev.publicplan.cloud/docker_proxy/debian
  script:
    - apt update
    - apt install git neofetch -y
    - neofetch
  only:
    - main
