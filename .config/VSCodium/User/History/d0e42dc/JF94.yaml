adminUser: admin
adminPassword: eix0phohgoo0leephaenee1Deughey9she4phaju4wueMugh

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: Loki
      type: loki
      url: http://efacluster-loki-gateway
      access: proxy
      # isDefault: true
      jsonData:
        timeout: 60
        maxLines: 1000
        httpHeaderName1: "X-Scope-OrgID"
      secureJsonData:
        httpHeaderValue1: "grafana"
      editable: true
    # deleteDatasources:
    # - name: Prometheus

ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
  ingressClassName: nginx
  hosts:
  - grafana.pre-stage.efa.publicplan.cloud
  tls:
  - secretName: grafana-pre-stage-efa-publicplan-cloud
    hosts:
      - grafana.pre-stage.efa.publicplan.cloud