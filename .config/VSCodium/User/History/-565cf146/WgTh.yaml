---
- name: Generate Ingress Nginx default values file
  ansible.builtin.copy:
    src: "{{ role_path }}/templates/default-values.yaml"
    dest: /tmp/nginx-default-values.yaml
    mode: '0640'

- name: Generate Ingress Nginx values file
  ansible.builtin.template:
    src: "{{ role_path }}/templates/values.yaml.j2"
    dest: /tmp/nginx-values.yaml
    mode: '0640'

- name: Add Ingress Nginx chart repo
  kubernetes.core.helm_repository:
    name: ingress-nginx
    repo_url: "https://kubernetes.github.io/ingress-nginx"

- name: Install Ingress Nginx via Helm
  kubernetes.core.helm:
    atomic: true
    kubeconfig: /etc/rancher/k3s/k3s.yaml
    chart_ref: ingress-nginx/ingress-nginx
    chart_version: "{{ ingress_nginx.chart_version }}"
    release_state: present
    release_name: ingress-nginx
    release_namespace: "{{ ingress_nginx.namespace }}"
    create_namespace: true
    values_files:
      - /tmp/nginx-default-values.yaml
      - /tmp/nginx-values.yaml
    wait: true
