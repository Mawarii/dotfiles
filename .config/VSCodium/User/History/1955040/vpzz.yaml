apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: domibus
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  labels:
    k8s-env: "develop"
spec:
  ingressClassName: "nginx"
  rules:
    - host: "domibus-test.develop.wspdev.de"
      http:
        paths:
          - pathType: ImplementationSpecific
            path: "/"
            backend:
              service:
                name: domibus
                port:
                  name: http
  tls:
    - hosts:
        - "domibus-test.develop.wspdev.de"
      secretName: domibus-tls
