apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - service.yaml
  - pvc_log.yaml
  - pvc_nginx.yaml
  - pvc_sdg_backend.yaml

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: gitlab-registry
    type: kubernetes.io/dockerconfigjson
    files:
      - .dockerconfigjson
