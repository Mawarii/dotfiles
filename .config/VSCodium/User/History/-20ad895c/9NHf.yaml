apiVersion: v1
kind: Service
metadata:
  name: empfangsclient-plus-client-service
spec:
  selector:
    app: empfangsclient-plus-client
  ports:
    - port: 8002
      targetPort: 8002
      name: http
