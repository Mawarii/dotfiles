apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sak-dp-server
spec:
  rules:
    - host: sak-dpa.dev.efa.publicplan.cloud
      http:
        paths:
          - backend:
              service:
                name: sak-dp-server
                port:
                  name: http
            path: /
            pathType: Prefix
