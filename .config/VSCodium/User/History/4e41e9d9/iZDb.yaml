apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

secretGenerator:
- name: keycloak-auth-fitko
  envs:
  - ./keycloak-auth.env
  options:
    disableNameSuffixHash: true
    annotations:
      replicator.v1.mittwald.de/replicate-to: 'empfangsclient-fitko'
      replicator.v1.mittwald.de/strip-labels: "true"
