include makefiles/config.Makefile
## custom/override config variables

include makefiles/docker.Makefile makefiles/php.Makefile
## custom/override definition and functions

# define up
# 	$(call validate)
# 	$(call dock_comp,up,-d,--remove-orphans,--quiet-pull)
# 	@printf "\033[32m%s\e[0m\n" 'Process complete, you can proceed and open http://${PROJECT_NAME}.${DOMAIN_SUFFIX} in browser'
# endef

init:
	$(MAKE) build
	$(MAKE) up
