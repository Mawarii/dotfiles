apiVersion: v1
kind: Service
metadata:
  name: empfangsclient-plus-client-service
spec:
  selector:
    app: empfangsclient-plus-client
  ports:
    - port: 3000
      targetPort: 3000
      name: http
