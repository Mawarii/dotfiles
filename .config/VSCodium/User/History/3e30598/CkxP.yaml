apiVersion: v1
kind: Service
metadata:
  name: drupal
spec:
  selector:
    app: drupal
    layer: public
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
