expose:
  tls:
    certSource: secret
    secret:
      secretName: "harbor.pre-stage.efa.publicplan.cloud-tls"
  ingress:
    hosts:
      core: harbor.pre-stage.efa.publicplan.cloud
    className: "nginx"