---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: efacluster-empfansclient-plus-apps
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: default
  sources:
    - directory:
        recurse: true
      path: ./argo-files
      repoURL: git@gitlab.publicplan.cloud:iac/efa/applications/empfangsclient-plus/develop.git
      targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
