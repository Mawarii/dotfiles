services:
  nocodb:
    image: {{ image.nocodb }}
    restart: unless-stopped
    container_name: nocodb
    environment: 
      NC_DB: {{ nocodb_database_connection }}
    expose:
      - "8080"
    networks:
      - proxy-net

networks:
  proxy-net:
    name: reverse_proxy
    external: true
