services:
  nocodb:
    image: {{ image.nocodb }}
    restart: unless-stopped
    container_name: nocodb
    environment: 
      NC_DB: "pg://root_db:5432?u=postgres&p=password&d=root_db"
    networks:
      - proxy-net

networks:
  proxy-net:
    name: reverse_proxy
    external: true
