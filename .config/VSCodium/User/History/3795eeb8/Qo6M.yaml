apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: govforms2-akeneo-version-ingress
  labels:
    k8s-env: "develop"
spec:
  ingressClassName: "nginx"
  rules:
    - host: "version.dev.efa.publicplan.cloud"
      http:
        paths:
          - pathType: Prefix
            path: "/govforms2-akeneo"
            backend:
              service:
                name: govforms2-akeneo-service
                port:
                  name: version
