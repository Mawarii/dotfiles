---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: empfangsclient-plus-dummy
  name: empfangsclient-plus-dummy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: empfangsclient-plus-dummy
  template:
    metadata:
      labels:
        app: empfangsclient-plus-dummy
    spec:
      containers:
        - name: dummy
          image: ghcr.io/shiftdigital-de/shiftdesk-dummy:canary
          env:
            - name: SERVER_URL
              value: "https://api.canary.empfangsclient-plus.de"
            - name: CLIENT_URL
              value: "https://canary.empfangsclient-plus.de"
            - name: WORKER_URL
              value: "https://worker.canary.empfangsclient-plus.de"
            - name: DUMMY_URL
              value: "https://dummy.canary.empfangsclient-plus.de"
            - name: RELEASE
              value: "canary"
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: shiftdesk-postgresql-app
                  key: uri
            - name: CIPHER_KEY
              valueFrom:
                secretKeyRef:
                  name: empfangsclient-plus-secret
                  key: cipher-key
            - name: AUTHORIZATION_KEY
              valueFrom:
                secretKeyRef:
                  name: empfangsclient-plus-secret
                  key: authorization-key
            - name: S3_USER
              valueFrom:
                secretKeyRef:
                  name: minio-secret
                  key: root-user
            - name: S3_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: minio-secret
                  key: root-password
            - name: S3_URL
              value: "$(S3_USER):$(S3_PASSWORD)@minio:9000"
