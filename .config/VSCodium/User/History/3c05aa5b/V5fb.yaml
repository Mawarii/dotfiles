apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: projectathon-frontend
  namespace: argocd
spec:
  destination:
    namespace: projectathon-frontend
    server: 'https://kubernetes.default.svc'
  project: efacluster-projectathon
  source:
    path: env/dev
    repoURL: 'git@gitlab.publicplan.cloud:regmo/infrastructure/oots-preview-space-dist.git'
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
