ingress:
  enabled: true
  hostname: pma-voapp.r.dev.publicplan.cloud
  annotations:
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: phpmyadmin-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required"
    nginx.ingress.kubernetes.io/whitelist-source-range: 213.61.255.112/29,10.130.0.0/16,172.16.0.0/16,10.140.0.0/16
  ingressClassName: "nginx"

db:
  allowArbitraryServer: false
  host: "devcluster-nextcloud-voapp-mariadb"
