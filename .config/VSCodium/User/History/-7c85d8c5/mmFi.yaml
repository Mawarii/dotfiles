---
- name: Create sshd configuration file
  ansible.builtin.template:
    src: ./templates/sshd_config.j2
    # dest: /etc/ssh/sshd_config.d/shield.conf
    dest: /tmp/shield.conf
    owner: root
    group: root
    mode: '0600'
