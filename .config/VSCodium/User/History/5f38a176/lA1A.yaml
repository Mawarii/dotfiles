apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matomo-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  labels:
    k8s-env: "develop"
spec:
  ingressClassName: "nginx"
  rules:
    - host: "matomo-old.develop.wspdev.de"
      http:
        paths:
          - pathType: ImplementationSpecific
            path: "/"
            backend:
              service:
                name: matomo-service
                port:
                  name: http
  tls:
    - hosts:
        - "matomo-old.develop.wspdev.de"
      secretName: matomo-tls
