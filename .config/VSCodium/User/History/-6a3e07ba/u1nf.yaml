---
- name: Check if default port is open
  ansible.builtin.wait_for:
    host: "{{ hostvars[item].ansible_host }}"
    port: "{{ ssh_port }}"
    timeout: 5
  ignore_errors: true
  register: port_check
  loop: "{{ groups['all'] }}"
