services:
  nginx:
    image: nginxinc/nginx-unprivileged:1.25-alpine
    container_name: cdn_nginx
    expose:
      - "8080"
    networks:
      - caddy-net

networks:
  caddy-net:
    name: caddy_proxy
    external: true
