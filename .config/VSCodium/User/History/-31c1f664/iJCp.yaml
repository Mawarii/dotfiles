apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/instance: govforms-frontend-copy
    gitlab.publicplan.cloud/primary-application-project-id: "45"
    gitlab.publicplan.cloud/primary-application-project-release-branch: staging
  name: govforms-frontend-copy
  namespace: argo-cd
spec:
  destination:
    namespace: copy-govforms-frontend
    server: 'https://kubernetes.default.svc'
  project: default
  source:
    path: applications/govforms-frontend/overlays/develop-copy/
    repoURL: 'git@gitlab.publicplan.cloud:themenwelt-wirtschaft/infrastruktur/common/app-configs.git'
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true