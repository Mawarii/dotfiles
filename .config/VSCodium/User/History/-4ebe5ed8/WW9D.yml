---
all:
  vars:
    ansible_python_interpreter: /usr/bin/python3

k3s_cluster:
  children:
    server:
      hosts:
        12.34.56.77:
    agent:
      hosts:
        12.34.56.88:
        12.34.56.99:

  vars:
    ## required vars
    k3s_version: v1.32.3+k3s1
    ssh_keys:
      - your
      - ssh
      - keys
    # the playbook will install ingress-nginx instead, so do not change
    extra_server_args: "--disable=traefik"

    ## optional vars
    # api_endpoint: "{{ hostvars[groups['server'][0]]['ansible_host'] | default(groups['server'][0]) }}"
    # kubeconfig: ~/.kube/demo

    # ssh_user: debian
    # ssh_port: 22222

    # cert_manager:
    #   chart_version: "v1.17.1"
    #   namespace: "cert-manager"

    # letsencrypt:
    #   mail: void@mawari.eu

    # ingress_nginx:
    #   chart_version: "4.12.1"
    #   namespace: "ingress-nginx"

    # wordpress:
    #   chart_version: "24.1.18"
    #   namespace: "wordpress"
