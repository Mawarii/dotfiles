---
- name: Run debug tasks
  hosts: all
  gather_facts: false
  become: true
  tasks:
    - name: DEBUG
      ansible.builtin.debug:
        msg: "{{ inventory_hostname is groups['controlplanes'][0]}}"
