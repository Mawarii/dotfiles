apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  creationTimestamp: null
  name: customercluster-pflegewegweiser-stage
  namespace: argocd
  annotations:
    k8s.publicplan.cloud/rbac-0: teschke@publicplan.de,exec
    k8s.publicplan.cloud/rbac-1: evertz@publicplan.de,exec
    k8s.publicplan.cloud/rbac-2: kufliievskyi@publicplan.de,exec
spec:
  destination:
    name: publicplan-customer
    namespace: pflegewegweiser-stage
  project: customercluster-pflegewegweiser
  sources:
  - ref: values
    repoURL: git@gitlab.publicplan.cloud:themenwelt-portale/pflegewegweiser-nrw-redesign-dist.git
    targetRevision: HEAD
  - path: env/stage
    repoURL: git@gitlab.publicplan.cloud:themenwelt-portale/pflegewegweiser-nrw-redesign-dist.git
    targetRevision: HEAD
  - chart: solr
    helm:
      valueFiles:
      - $values/helm/stage/solr/default-values.yaml
      - $values/helm/stage/solr/values.yaml
    repoURL: https://charts.bitnami.com/bitnami
    targetRevision: 9.4.0
  - chart: mariadb
    helm:
      valueFiles:
      - $values/helm/stage/bitnami-mariadb/default-values.yaml
      - $values/helm/stage/bitnami-mariadb/values.yaml
    repoURL: registry-1.docker.io/bitnamicharts
    targetRevision: 20.2.1
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
