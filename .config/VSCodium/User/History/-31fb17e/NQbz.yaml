---
- name: Add firewall for public communication
  hetzner.hcloud.firewall:
    api_token: "{{ hcloud_token }}"
    name: "k8s_pub"
    rules:
      - port: "{{ ssh_port }}"
        protocol: tcp
        direction: in
        source_ips:
          - 0.0.0.0/0
    state: present
