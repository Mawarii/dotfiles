apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: efacluster-ip-oots-frontend
  namespace: argocd
spec:
  destination:
    namespace: intermediaryplatform
    name: in-cluster
  project: efacluster-intermediaryplatform
  sources:
  - path: env/dev
    repoURL: 'git@gitlab.publicplan.cloud:iac/efa/distributions/intermediaryplatform/oots_frontend-dist.git'
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
