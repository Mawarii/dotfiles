apiVersion: v1
kind: Service
metadata:
  name: static
spec:
  selector:
    app: static
    layer: public
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
    - protocol: TCP
      port: 873
      targetPort: 1873
      name: rsync
  type: ClusterIP
