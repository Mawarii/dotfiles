source:
  repo:
    kind: HELM
    url: https://charts.bitnami.com/bitnami

target:
  # Container images registry authn
  containers:
    url: https://charts.bitnami.com/bitnami
    auth:
      username: "USERNAME"
      password: "PASSWORD"
  repo:
    kind: LOCAL
    path: ./charts
    # Helm repository authentication
    # auth:
    #   username: "USERNAME"
    #   password: "PASSWORD"
charts:
  - nginx