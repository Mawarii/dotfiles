---
- name: Run debug tasks
  hosts: all
  gather_facts: false
  become: false
  tasks:
    - name: DEBUG
      ansible.builtin.get_url:
        url: https://get.k3s.io
      delegate_to: localhost
      changed_when: true
