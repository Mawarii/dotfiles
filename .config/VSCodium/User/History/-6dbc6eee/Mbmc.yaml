---
- name: Run debug tasks
  hosts: all
  gather_facts: false
  become: false
  tasks:
    - name: DEBUG
      ansible.builtin.debug:
        msg: "{{ ansible_host }}"
  ignore_errors: true
  delegate_to: localhost
