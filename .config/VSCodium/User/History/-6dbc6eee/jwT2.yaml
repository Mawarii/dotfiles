---
- name: Run debug tasks
  hosts: all
  gather_facts: false
  become: false
  tasks:
    - name: DEBUG
      ansible.builtin.debug:
        msg: "{{ groups['server'][0] }}"
      delegate_to: localhost
