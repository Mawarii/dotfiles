apiVersion: v1
kind: Service
metadata:
  name: empfangsclient-plus-server-service
spec:
  selector:
    app: empfangsclient-plus-server
  ports:
    - port: 3000
      targetPort: 3000
      name: http
