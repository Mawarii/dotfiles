apiVersion: v1
kind: Service
metadata:
  name: empfangsclient-plus-server-service
spec:
  selector:
    app: empfangsclient-plus-server
  ports:
    - port: 8000
      targetPort: 8000
      name: http
