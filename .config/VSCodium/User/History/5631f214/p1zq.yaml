apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/instance: prometheus
  name: prometheus
  namespace: argo-cd
spec:
  destination:
    namespace: monitoring
    server: 'https://kubernetes.default.svc'
  project: default
  sources:
  - path: applications/monitoring/overlays/staging/resources
    repoURL: 'https://gitlab.krz.de/customers/publicplan/wsp/staging/app-configs.git'
    targetRevision: HEAD
  - chart: kube-prometheus
    helm:
      valueFiles:
      - $values/applications/monitoring/overlays/staging/operator-values.yaml
    repoURL: http://chartmuseum.chartmuseum.svc:8080
    targetRevision: "9.5.2"
  - chart: prometheus
    helm:
      valueFiles:
      - $values/applications/monitoring/overlays/staging/prometheus-values.yaml
    repoURL: https://charts.bitnami.com/bitnami
    targetRevision: "1.3.8"
  - ref: values
    repoURL: 'https://gitlab.krz.de/customers/publicplan/wsp/staging/app-configs.git'
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
