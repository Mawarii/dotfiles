image:
  repository: gitlab.publicplan.cloud:5050/themenwelt-kommunales/nextcloud-server
  tag: dcf5ef2f
  pullSecrets:
    - gitlab-registry-nextcloud-server

ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 4G
    nginx.ingress.kubernetes.io/server-snippet: |-
      server_tokens off;
      proxy_hide_header X-Powered-By;
      rewrite ^/.well-known/webfinger /index.php/.well-known/webfinger last;
      rewrite ^/.well-known/nodeinfo /index.php/.well-known/nodeinfo last;
      rewrite ^/.well-known/host-meta /public.php?service=host-meta last;
      rewrite ^/.well-known/host-meta.json /public.php?service=host-meta-json;
      location = /.well-known/carddav {
        return 301 $scheme://$host/remote.php/dav;
      }
      location = /.well-known/caldav {
        return 301 $scheme://$host/remote.php/dav;
      }
      location = /robots.txt {
        allow all;
        log_not_found off;
        access_log off;
      }
      location ~ ^/(?:build|tests|config|lib|3rdparty|templates|data)/ {
        deny all;
      }
      location ~ ^/(?:autotest|occ|issue|indie|db_|console) {
        deny all;
      }

nextcloud:
  host: nc-0.voapp.dev.publicplan.cloud
  username: admin
  password: szztPmXWHOKQx7IvrVLZr9Sg26tgAaJb6wIgEG2Wy0TkkjH2W1hpDbB51ptVVn9u

internalDatabase:
  enabled: false

externalDatabase:
  enabled: true
  type: mysql
  host: "devcluster-nextcloud-voapp-mariadb"
  user: nextcloud
  password: "IaPt0gSX66ogY4CqcK9OedZqt5N4oCn7"
  database: nextcloud

rbac:
  enabled: true
