---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: etr-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  labels:
    k8s-env: "develop"
spec:
  ingressClassName: "nginx"
  rules:
    - host: "etr.develop.wspdev.de"
      http:
        paths:
          - pathType: ImplementationSpecific
            path: "/"
            backend:
              service:
                name: etr
                port:
                  name: http
  tls:
    - hosts:
        - "etr.develop.wspdev.de"
      secretName: etr-tls
