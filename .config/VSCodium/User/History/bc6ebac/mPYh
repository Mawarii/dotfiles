---
all:
  vars:
    ansible_python_interpreter: /usr/bin/python3

nodes:
  server:
    hosts:
      157.90.161.231:
  agent:
    hosts:

  vars:
    ansible_port: "{{ ssh_port }}"
    ansible_user: "{{ ssh_user }}"

    # Required vars
    ssh_keys:
      - add
      - your
      - ssh-keys
