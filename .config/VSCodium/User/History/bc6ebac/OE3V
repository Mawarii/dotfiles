---
all:
  vars:
    ansible_python_interpreter: /usr/bin/python3

k3s_cluster:
  children:
    server:
      hosts:
        192.16.35.11:
    agent:
      hosts:
        192.16.35.12:
        192.16.35.13:

  vars:
    ansible_port: "{{ ssh_port }}"
    ansible_user: "{{ ssh_user }}"
