---
all:
  vars:
    ansible_python_interpreter: /usr/bin/python3

nodes:
  children:
    server:
      hosts:
        192.16.35.11:

  vars:
    ansible_port: "{{ ssh_port }}"
    ansible_user: "{{ ssh_user }}"

    # Required vars
    ssh_keys:
      - add
      - your
      - ssh-keys
