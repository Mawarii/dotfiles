---
# Source: matomo/templates/svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: matomo
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
    - port: 3000
      targetPort: 3000
      name: version
  selector:
    app.kubernetes.io/instance: matomo
    app.kubernetes.io/name: matomo
