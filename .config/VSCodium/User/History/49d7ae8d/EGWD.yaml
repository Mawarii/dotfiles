apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysqlvol
  labels:
    app: domibus
spec:
  storageClassName: "default"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
