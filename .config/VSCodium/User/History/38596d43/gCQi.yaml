apiVersion: batch/v1
kind: CronJob
metadata:
  name: govforms2-akeneo-import-job
spec:
  schedule: "*/5 * * * *"
  suspend: true
  jobTemplate:
    spec:
      template:
        spec:
          imagePullSecrets:
            - name: gitlab-registry-govforms2-akeneo
