apiVersion: v1
kind: ConfigMap
metadata:
  name: empfangsclient-eco-configmap
data:
  MARIADB_HOSTNAME: "mariadb-eco:3306"

  ADMINER_DEFAULT_SERVER: "mariadb"

  FITCONNECT_SDK_ENVIRONMENT_NAME: "TEST"

  ECC_API_BASE_URL: "http://empfangsclient-ecc.empfangsclient-fitko.svc.cluster.local:9443"

  SECURE_STORAGE_SERVICE: "vault"
  SECURE_STORAGE_VAULT_HOST: "empfangsclient-fitko-vault-server"
  SECURE_STORAGE_VAULT_PORT: "8200"
  SECURE_STORAGE_VAULT_SCHEME: "http"
  SECURE_STORAGE_VAULT_PATH: "ec"
  SECURE_STORAGE_VAULT_KUBERNETES_ROLE: "eco-role"

  KEYCLOAK_TRUSTEDURLS: "https://keycloak-fitko.efa-stage.owl-it.de"
  KEYCLOAK_ECOBASEURL: "https://eco-fitko.efa-stage.owl-it.de"

  SENTRY_DSN: "https://3d2a73512af76b5654bdc017b7500d3a@sentry.live.publicplan.cloud/35"
  SENTRY_ENVIRONMENT: "staging"
  SENTRY_TRACESSAMPLERATE: "1.0"

  # Disabled Logfile
  # LOGGING_FILE_NAME: "/var/log/spring-${ECO_POD_NAME}.log"
  # LOGGING_LOGBACK_ROLLINGPOLICY_MAXHISTORY: "90"
