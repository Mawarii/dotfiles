apiVersion: apps/v1
kind: Deployment
metadata:
  name: govforms2-client
  labels:
    k8s-env: pre-stage
spec:
  template:
    spec:
      imagePullSecrets:
        - name: gitlab-registry-govforms2-client
        - name: gitlab-registry-version-sidecar
