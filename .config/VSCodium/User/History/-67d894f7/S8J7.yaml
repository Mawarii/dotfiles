apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: empfangsclient-eco-version-ingress
  labels:
    k8s-env: "pre-stage"
spec:
  ingressClassName: "nginx"
  rules:
    - host: "version.efa-stage.owl-it.de"
      http:
        paths:
          - pathType: Prefix
            path: "/empfangsclient-eco-zdi"
            backend:
              service:
                name: empfangsclient-eco
                port:
                  name: version
