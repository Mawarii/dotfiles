# docker run -v ./config.yaml:/config.yaml gcr.io/bitnami-labs/charts-syncer:v0.20.3 sync -c /config.yaml --skip-dependencies
stages:
  - sync

sync:
  stage: sync
  image:
    name: gcr.io/bitnami-labs/charts-syncer:v0.20.3
    entrypoint: [""]
  script:
    - /charts-syncer sync
      -c "${CI_PROJECT_DIR}/config.yaml"
      --skip-dependencies
  only:
    - main
