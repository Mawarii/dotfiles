stages:
  - sync

sync:
  stage: sync
  image:
    name: gcr.io/bitnami-labs/charts-syncer:v0.20.3
  script:
    - sync
      -c "${CI_PROJECT_DIR}/config.yaml"
      --skip-dependencies
  only:
    - main
